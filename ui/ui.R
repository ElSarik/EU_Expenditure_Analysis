# Define UI for application that draws a histogram
ui <- fluidPage(
  
  # Application title
  titlePanel("An Analysis On Each EU Country's Annual Expenditure"),
  'Based on the data publicly available from Eurostat, 
  together with the COFOG Classification.',
  
)